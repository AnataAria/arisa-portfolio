services:
  portfolio-client:
    image: ${DOCKER_USERNAME}/portfolio-client:latest
    build:
      context: nextjs-client
      dockerfile: Dockerfile
      args:
        PUBLIC_API_ENDPOINT: ${PUBLIC_API_ENDPOINT}
  portfolio-server:
    image: ${DOCKER_USERNAME}/portfolio-server:latest
    build:
      context: rust-server
      dockerfile: Dockerfile
